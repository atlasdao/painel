# PostgreSQL Client Authentication Configuration File
# ===================================================

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            md5

# IPv4 remote connections - Allow all IPs (configure firewall for security)
host    all             all             0.0.0.0/0               md5

# IPv6 local connections
host    all             all             ::1/128                 md5

# IPv6 remote connections - Allow all IPs
host    all             all             ::/0                    md5

# Special rules for specific users
host    atlas_db        atlas           0.0.0.0/0               md5
host    all             erick           0.0.0.0/0               md5

# Replication connections (if needed later)
# host    replication     replicator      0.0.0.0/0               md5